# DAY 2

## DIZIONARI

NON ordinati, sono mutable ( i valori ) ma non ci devono essere delle chiavi duplicate ( che non possono essere modificate )

funzione zip: spiegazione + esempio

## FUNZIONI CON PARAMETRI OPZIONALI ( migliorare e fare esempi per comprendere )

argomenti formali ( quelli classici )

parametri di lunghezza variabile si aggiunge *

Posso avere parametri con **

bisogna rispettare ordine: FORMALI, *args, **kwargs

## INTROSPEZIONE 

### Esempio con la lista
l = []
type(l)
isinstance(object, type) che restituisce true o false


Attributi ( variabili interne ) e metodi ( funzioni )

hasattr(l , 'append') # True
hasattr(l , 'items') # False

callable(l.append) per capire se è una funzione

## liberia standard

### modulo OS

## modulo SHUTIL

## modulo PATHLIB

## modulo LOGGING

## modulo ARGPARSE

